#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <math.h>

// 1、给定一个单链表，判断链表是否存在环路

// 2、输入一棵二叉树和一个整数时，能打印出二叉树中结点值的和为输入整数的所有路径

// 3、一个有向无环图的根节点是指某个节点 R 通过它出发的所有有向路径可以到达图的任意一个节点。设计算法使一个有向图作为输入，假如有根，确认图的根节点